# Machine Learning Pipeline

This document focuses on the machine learning component of NewsChecker, including the dataset, model selection, training process, evaluation, and current limitations. The preprocessing pipeline and overall application architecture are documented in the README.

## Table of Contents

- [Dataset](#dataset)
- [Model](#model)
- [Evaluation](#evaluation)
- [Limitations](#limitations)
- [Future Improvements](#future-improvements)

## Dataset

NewsChecker was trained using a publicly available Kaggle dataset containing URLs to news articles labeled for fake news detection. Each article is assigned a binary label, where 0 represents fake news and 1 represents real news. The dataset can be found [here.](https://www.kaggle.com/datasets/algord/fake-news)

Although the original dataset contained 21,724 unique articles, only 12,879 articles were successfully processed by the data collection pipeline. The remaining articles were excluded due to issues encountered during article retrieval and extraction, such as failed HTTP requests or cases where the pipeline was unable to extract usable article text. Only the processed articles were used for training because the excluded articles did not produce the text required for embedding generation and model training.

The 12,879 successfully processed articles were split into three separate datasets:

- **Training set: 9,015 articles (70%)**
- **Validation set: 1,931 articles (15%)**
- **Testing set: 1,933 articles (15%)**

The training set was used to train the model, the validation set was used to evaluate the model during development, and the test set was used to evaluate how well the model performs on data it had never seen during training.

### Class Distribution

The processed dataset contains 2,645 fake news articles and 10,234 real news articles. As a result, the dataset is imbalanced, with substantially more real news articles than fake news articles. The table below shows the number of fake and real news articles in each dataset:

| Dataset    | Fake Articles (0) | Real Articles (1) | Total  |
| :-----:    | :---------------: | :---------------: | :---:  |
| Training   | 1,826             | 7,189             | 9,015  |
| Validation | 440               | 1,491             | 1,931  |
| Testing    | 379               | 1,554             | 1,933  |
| Total      | 2,645             | 10,234            | 12,879 |

Because the dataset contains significantly more real news articles than fake news articles, the classes are imbalanced. As a result, overall accuracy does not provide a complete picture of the model's performance, so additional evaluation metrics were used.

## Model

### Model Selection

NewsChecker uses a Logistic Regression model to classify news articles as either trustworthy or untrustworthy.

#### Why Logistic Regression?

Logistic Regression was chosen because it is a strong baseline model for binary classification problems like determining whether a news article is trustworthy or untrustworthy. It is simple to train, makes predictions quickly, and works well with dense sentence embeddings such as those generated by the `all-MiniLM-L6-v2` SentenceTransformer model.

Another advantage of Logistic Regression is that it produces a probability score for every prediction instead of only a binary label. This allowed NewsChecker to present users with confidence levels (such as "Probably Trustworthy" or "Likely Untrustworthy") rather than only classifying articles as trustworthy or untrustworthy.

### Model Input

Instead of using raw article text as input, the model uses **semantic embeddings** generated by the **all-MiniLM-L6-v2** SentenceTransformer model.

During preprocessing, each article is divided into multiple text segments. If an article contains more than 4,000 segments, a stride-based sampling method is used to select 4,000 segments while maintaining coverage of the article's content.

Each segment is then tokenized and passed through the all-MiniLM-L6-v2 model to generate a vector representation for each token. An **attention mask** is used to distinguish actual tokens from padding tokens, and mean pooling is applied across the token embeddings to produce a single **384-dimensional embedding for each segment**.

The segment embeddings are then averaged across the entire article to produce a single **384-dimensional embedding** representing the article as a whole. This final article-level embedding is used as the input to the **Logistic Regression** classifier.

### Training

The model was trained using **scikit-learn's** `LogisticRegression` class.

The following hyperparameters were used during training:
- `max_iter=1000` to give the model enough opportunities to learn from the training data before stopping.
- `class_weight='balanced'` to account for the dataset's imbalance between fake and real news articles (approximately 20.5% fake and 79.5% real), helping the model learn from both classes more fairly.

### Predictions

For each article, the model produces:
- A binary prediction indicating whether the article is trustworthy or untrustworthy.
- A confidence score, generated using `predict_proba()`, that estimates how confident the model is that an article is trustworthy.

The confidence score is displayed using the following trustworthiness labels:
- 0.0 – 0.2: Likely Untrustworthy
- 0.2 – 0.4: Probably Untrustworthy
- 0.4 – 0.6: Uncertain
- 0.6 – 0.8: Probably Trustworthy
- 0.8 – 1.0: Likely Trustworthy

### Saving the Model

After training, the model is saved using **joblib.** This allows the trained model to be loaded later for predictions without needing to train it again.

### Performance

Making a prediction for a news article usually takes a few seconds. Most of this time is spent generating sentence embeddings using the `all-MiniLM-L6-v2` model. Once the embedding has been created, the Logistic Regression model makes its prediction almost instantly.

## Evaluation

The trained model was evaluated using a test dataset of 1,933 articles. Of those articles, 379 (19.6%) were fake news articles and 1,554 (80.4%) were real news articles. The model's performance was evaluated using the following classification metrics: accuracy, precision, recall, F1-score, and ROC-AUC.

### Metric Interpretation

#### Accuracy (74.13%)

The model correctly predicted 74.13% of the articles in the test dataset, indicating that it can accurately classify new articles.

#### Precision

- Class 0 (Fake News) Precision: 0.41 — Of all the articles that the model predicted to be fake news, 41% were actually fake news, while the remaining 59% were actually real news. This indicates that the model frequently misclassifies real news articles as fake news.
- Class 1 (Real News) Precision: 0.91 — Of all the articles that the model predicted to be real news, 91% were actually real news, while the remaining 9% were actually fake news. This indicates that when the model predicts an article as real news, it is usually correct.

#### Recall

- Class 0 (Fake News) Recall: 0.71 — Of the 379 fake news articles in the test dataset, the model correctly identified approximately 71% of them as fake.
- Class 1 (Real News) Recall: 0.75 — Of the 1,554 real news articles in the test dataset, the model correctly identified approximately 75% of them as real.

#### F1-Score

- Class 0 (Fake News) F1-Score: 0.52 — The model achieved moderate overall performance for fake news classification. While it correctly identified approximately 71% of fake news articles in the test dataset, its lower precision score of 41% shows that a significant portion of its fake news predictions were incorrect, resulting in a lower overall F1-score.
- Class 1 (Real News) F1-Score: 0.82 — The model achieved strong overall performance for real news classification. It correctly identified approximately 75% of the real news articles in the test dataset. Additionally, when the model predicted an article as real news, 91% of those predictions were correct, meaning that most real news predictions were reliable. These results demonstrate that the model performed better when classifying real news compared to fake news, achieving an F1-score of 0.82 compared to 0.52 for fake news.

#### ROC-AUC (0.8022)

The model achieved a ROC-AUC score of 0.8022, indicating that it can effectively distinguish between fake and real news articles based on their predicted probabilities. This means that the model assigns a higher probability score to a randomly selected real news article than to a randomly selected fake news article approximately 80.22% of the time.

### Confusion Matrix

|                  | Predicted Fake News | Predicted Real News |
| :--------------: | :-----------------: | :-----------------: |
| Actual Fake News | 269                 | 110                 |
| Actual Real News | 390                 | 1,164               |

### Analysis

The evaluation results show that the NewsChecker classifier was able to successfully learn patterns from the training data and apply them to articles that were not included during training. The model performed particularly well when identifying Class 1 (Real News) articles, demonstrating strong performance for this class.

However, the classifier was less effective at identifying Class 0 (Fake News) articles. This difference in performance may be partially explained by the distribution of the test dataset, which contained substantially more Class 1 (Real News) articles than Class 0 (Fake News) articles. Because the model had more examples from Class 1 (Real News) available during training, it may have learned stronger patterns for identifying that class.

The evaluation results show that the data preparation, feature extraction, and model training pipeline allowed the classifier to learn patterns that help distinguish between the two classes. However, the incorrect predictions show that further improvements are needed, especially to reduce errors when classifying Class 0 (Fake News) articles.

## Limitations

Although the model demonstrates reasonable performance on unseen articles, several limitations remain.

First, the model's effectiveness depends heavily on the quality and diversity of the training dataset. While the dataset includes articles from a variety of news sources and domains, the labels were created by the original dataset author. Additionally, the dataset's coverage of different topics and languages is not fully documented, making it unclear how well the model performs on different types of news content.

Second, the classifier evaluates articles based only on their textual content. Although sentence embeddings allow the model to understand the semantic meaning of an article, the system does not verify claims using external sources or compare information with other news articles. Therefore, the model functions as a content-based classifier rather than a complete fact-checking system.

Finally, the model has limited explainability. Because predictions are based on patterns learned from article text, the model cannot identify or communicate the specific factors that led to a particular classification. As a result, users may not understand why an article was classified as real or fake.

## Future Improvements

There are several improvements that I would like to make to NewsChecker in the future.

First, the training pipeline could be refined to produce higher-quality training data. During development, several issues were identified that prevented certain articles from being processed correctly. Improving the preprocessing pipeline and regenerating the training data would likely produce a cleaner dataset and improve the model's overall performance.

Second, the model itself could be improved by experimenting with different classifiers and embedding models. Additionally, continuously retraining the model with newer articles, expanding the dataset to include more languages, and maintaining a more balanced distribution of real and fake news articles could improve the model's ability to accurately classify articles it has never seen before.

Third, I would like to expand the model by using additional input features during the prediction process. Currently, the model uses only the text of an article as input when making a prediction. In the future, I would like to provide the model with additional input features, such as publisher credibility, domain reputation, author information, and other relevant metadata, to improve the accuracy of its predictions.

Finally, I would like to improve the overall user experience by making the model's predictions easier to understand. While the application currently displays a truthfulness label and a confidence score, it does not explain why the model reached its conclusion. In the future, I would like the application to provide users with an explanation of the factors that most influenced each prediction, allowing them to better understand and interpret the results. Additionally, I would like to further refine the web application's user interface by creating a more modern, polished, and visually appealing design.